# HPE CSI Driver for Kubernetes 3.2.0 Release Notes

## HPE CSI Driver for Kubernetes 3.2.0

| **Version:** |3.2.0|
|--------------|-----|
| **Revision:** | Wednesday June 10, 2026 |

## Resolved Issues

The following table lists the resolved issues for HPE CSI Driver for Kubernetes v3.2.0.

| ID |Component |Title|Resolution |Escalation |
|----|----------|-----|-----------|-----------|
|CON-3935|csp.x10kfile|Added a new CSP for Alletra MP X10000 File Support|Added a new CSP for Alletra MP X10000 File Support||
|CON-4697|csp.3par|Openshift AAPP VM with RCG created through CSI is not added properly to RCG|Mark an online clone as Completed when no replication is requested||
|CON-4658|csp.3par|Unable to migrate OpenShift VMs between nodes and causing VM Migrations fails|Fixed VM migration issues between OpenShift nodes|ESC-17465|
|CON-4642|csp.3par|RWX volume has type "host" when hostSeesVLUN is false|Fixed VLUN type assignment for RWX volumes with hostSeesVLUN set to false||
|CON-4636|csp.b10kfile|Session renewal is not working if we restart the B10000 file CSP|Fixed session management and renewal after CSP restart||
|CON-4614|csp.3par|dataSourceAsPVC/Snapshot-Restore to new PVC fails on Replicated Non-Alletra MP volumes|Fixed snapshot restore operations for Classic Peer Persistence replicated volumes|ESC-17386|
|CON-4609|csp.3par|Primera CSP panics and restarts when it receives busy response from the backend|Added proper error handling for HTTP 503 "service unavailable" responses from backend|ESC-17163|
|CON-4605|csp.b10kfile|File share CSI token call not able to unmarshal the response properly|Fixed JSON unmarshalling for session token responses||
|CON-4581|csp.b10kfile|File shares are getting mounted using nolock and vers=4, which could lead to data corruption|Removed nolock mount option for NFSv4 to prevent potential data corruption||
|CON-4576|csp.3par|APP volumes multipaths with hostSeesVLUN set to true are discovered only from primary array|Fixed multipath discovery to include paths from both primary and secondary arrays||
|CON-4561|csp.b10kfile|Pod Deployment failing in some cases|Fixed compatibility issues for B10000 File Storage||
|CON-4525|csp.3par|Classic PP vluns are not deleted from secondary array in Primera/Arcus|Fixed VLUN cleanup on secondary array for Classic Peer Persistence volumes||
|CON-4496|csp.3par|CSI Driver 3.0.2 – CSP returns success for PublishVolume when volume is exported to a different node|Added node validation before returning publish success|ESC-17192|
|CON-4484|csp.3par|VolumeSnapshot delete does not delete array snapshot|Fixed snapshot deletion to properly clean up array-side snapshots||
|CON-4471|csp.3par|Recursive CSP POD Crash due CSP trying to find non-existent task ID on array|Added proper error handling for non-existent task IDs on array|ESC-17163|
|CON-4465|csp.3par|NVMe with multiple vlan tagging for all ports appends * to ips during discovery and fails|Fixed NVMe discovery to handle multiple VLAN-tagged ports correctly||
|CON-4448|csp.3par|Invalid replicationPolicy still falls back to classic PP in Arcus|Added validation to error out on invalid replicationPolicy values for Arcus arrays||
|CON-4441|csi.k8s|Inline ephemeral scrubber traversing entire filesystems|Fixed scrubber to only traverse CSI-managed ephemeral volume mount points||
|CON-4429|csp.3par|Snapshotting a replicated statically provisioned PVC does not replicate snapshots|Fixed snapshot replication for statically provisioned replicated PVCs||
|CON-4369|csp.3par|Active PP replicated volumes are not deleted upon pvc deletion when port 443 is not specified in the secret configuration|Fixed replicated volume deletion without requiring port 443 in secret||
|CON-4254|csp.3par|Replicated snapshot is not deleted from the secondary array|Updated the Snapshot deletion workflow to delete the Snapshot from the Peer array||

## Known Issues

Please note that, `Known Issues` from previous releases are still applicable with suggested workarounds, if they are not part of `Resolved Issues` above.

| ID | Component | Title | Description | Workaround |
|----|-----------|-------|-------------|------------|
|CON-4216|csp.3par|Host creation with PP migration will have different names on both arrays|When upgrading CSI driver from v2.5.2 to v3.0.0/v3.1.0, a host naming mismatch occurs between primary and secondary arrays for replicated volumes, causing unpublish to fail|Manually align the host name and CRD entries so that both arrays and the CRD use the same host name convention|
|CON-4387|csp.3par|NVMe connectivity/paths are not re-established after array reboot|If array reboot takes a lot of time, the hosts are not able to re-establish NVMe connectivity|Scaling down the deployment and scaling up will re-establish the connection|
|CON-4430|csp.3par|CSI3.1-Openshift-VVSet not removed after deleting the volumegroup in array side|VVSet not removed after deleting the volumegroup in array side||
|CON-4438|csp.3par|Ephemeral inline pods using more than one volume, fail when pod names are longer|Long ephemeral Pod names cause pvc names to be truncated and cause issues, when ephemeral pod has more than 1 volume|Issue is not seen if Ephemeral Pod Names are limited to 11 characters|
|CON-4534|csp.3par|Restrict replicated PVCs for volumegroup(vvset) and snapshotgroup related operations|CSP is not restricting adding replicated PVCs into volumegroup(vvset) and into snapshotgroup resulting into various issues||
|CON-4668|csp.3par|unable to clone PVC to higher size using cloneOf parameter|Cloning PVC to the higher size from parent PVC fails||
|CON-4723|csp.b10kfile|Pod consuming B10000 file share failed after CSI upgrade from 3.1.0 to 3.2.0-beta|Pods consuming B10000 file shares may fail after upgrading CSI driver|Pods can be migrated to a different node, or in case of Deployments, scale them down to 0 and back|
|CON-4656|csp.3par|ESC-17504: CSP is enforcing the minumum size of reduced volumes as 16Gb|When provisioning PVC with provisioningType:reduce, and size < 16 GiB on an Alletra MP array, the CSP silently overrides the requested size and provisions a 16 GiB volume instead|None|
|CON-4742|csi.k8s|ESC-17589: Unintended detachment/Re-enumeration of Unrelated Multipath Device During CSI PVC Detach/Attach|Unrelated multipath device gets detached during CSI PVC Detach/Attach on RHEL9.x systems|Use different LUN IDs for non CSI Volumes|
