apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-raw-pvc-1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: my-sc-1
  volumeMode: Block
