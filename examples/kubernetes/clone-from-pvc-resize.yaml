apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clone-of-pvc-1-resized
  namespace: kube-system
spec:
  dataSource:
    name: pvc-1
    kind: PersistentVolumeClaim
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: sc-1
